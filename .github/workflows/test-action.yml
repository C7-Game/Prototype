name: Test Action
on:
  workflow_dispatch:
    inputs:
      build-type:
        type: choice
        description: This selection does nothing yet
        options:
        - debug
        - release
      folder-suffix:
        description: Will be added to "C7" for folder/file name
        default: -dev-build-${{ github.actor }}
        required: false
jobs:
  do-a-thing:
    runs-on: ubuntu-latest
    steps:
      - name: Make a tar archive
        run: |
          echo "I did a thing"
          echo "Build type is ${{ inputs.build-type }}"
          echo "Folder/file names will be C7${{ inputs.folder-suffix }}"
